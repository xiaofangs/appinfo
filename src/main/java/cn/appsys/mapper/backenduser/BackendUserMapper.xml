<?xml version="1.0" encoding="UTF-8" ?> <!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.appsys.mapper.backenduser.BackendUserMapper">
	<select id="BackUserLogin"  resultType="BackendUser">
	  select u.* from backend_user as u
	   where userCode = #{userCode} 
    </select>
</mapper>
